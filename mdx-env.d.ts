// Types the `frontmatter` export injected into every MDX module by
// remark-mdx-frontmatter, so referencing it in page.mdx is typed (not implicit any).
declare const frontmatter: Record<string, unknown>
